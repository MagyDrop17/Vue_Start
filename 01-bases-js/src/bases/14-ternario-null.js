
let firstName;
let lastName = 'Smith';

/* console.log(`${firstName || 'no firstName'} ${lastName || 'no lastName'}`); */

const isActve = true

const message = (isActve) 
                    ? 'Active' 
                    : 'Inactive'

console.log(message);